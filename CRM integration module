const axios = require('axios');

// Function to generate quote
async function generateQuote(enquiryData) {
    let response = await axios.post('https://your-crm-api.com/generate-quote', enquiryData, {
        headers: {
            'Authorization': 'Bearer YOUR_API_KEY'
        }
    });

    return response.data;
}
